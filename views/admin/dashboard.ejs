<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="dashboard-container">
        <header class="dashboard-header">
            <h1>Dashboard - <span id="business-name">Não quero</span></h1>
            <div class="user-actions">
                <span id="user-greeting">Olá, Rodrigo!</span>
                <button onclick="logout()" class="btn-logout">Sair</button>
            </div>
        </header>

        <div class="dashboard-grid">
            <!-- Status WhatsApp Card -->
            <div class="card">
                <h3>Status WhatsApp</h3>
                <div id="whatsapp-status" class="status disconnected">
                    <span>Desconectado</span>
                </div>
                
                <div id="qrcode-container" class="hidden">
                    <h4>Conectar WhatsApp</h4>
                    <img id="qrcode" src="" alt="QR Code para conectar WhatsApp">
                    <p>Escaneie este QR Code com seu WhatsApp</p>
                    <div class="instructions">
                        <p><strong>Como conectar:</strong></p>
                        <ol>
                            <li>Abra o WhatsApp no seu celular</li>
                            <li>Toque em <strong>Menu → Dispositivos conectados</strong></li>
                            <li>Toque em <strong>Conectar um dispositivo</strong></li>
                            <li>Escaneie o QR Code acima</li>
                        </ol>
                    </div>
                </div>

                <div id="success-container" class="hidden">
                    <div class="success-icon">✅</div>
                    <div class="success-message">WhatsApp Conectado!</div>
                    <p>O bot está pronto para receber mensagens</p>
                </div>

                <button id="connect-btn" class="btn-primary" onclick="connectWhatsApp()">Conectar WhatsApp</button>
            </div>

            <!-- Configurações do Negócio -->
            <div class="card">
                <h3>Configurações do Negócio</h3>
                <form id="business-config-form">
                    <div class="form-group">
                        <label for="business-name-input">Nome do Negócio</label>
                        <input type="text" id="business-name-input" name="businessName" required>
                    </div>
                    <div class="form-group">
                        <label for="business-type">Segmento</label>
                        <select id="business-type" name="businessType">
                            <option value="varejo">Varejo</option>
                            <option value="servicos">Serviços</option>
                            <option value="restaurante">Restaurante</option>
                            <option value="imoveis">Imóveis</option>
                            <option value="outros">Outros</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="welcome-message">Mensagem de Boas-Vindas</label>
                        <textarea id="welcome-message" name="welcomeMessage" rows="3" placeholder="Digite a mensagem de boas-vindas para seus clientes..."></textarea>
                    </div>
                    <button type="submit" class="btn-primary">Salvar Configurações</button>
                </form>
            </div>

            <!-- Menu de Atendimento -->
            <div class="card">
                <h3>Menu de Atendimento</h3>
                <div id="menu-options-container">
                    <!-- As opções serão carregadas via JavaScript -->
                </div>
                <button onclick="addMenuOption()" class="btn-secondary" style="background: #666; color: white; padding: 10px; border: none; border-radius: 5px; cursor: pointer; width: 100%; margin-top: 10px;">
                    + Adicionar Opção
                </button>
            </div>

            <!-- Estatísticas -->
            <div class="card">
                <h3>Estatísticas</h3>
                <div class="stats-grid">
                    <div class="stat-item">
                        <span class="stat-number" id="total-conversations">0</span>
                        <span class="stat-label">Conversas Hoje</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-number" id="total-customers">0</span>
                        <span class="stat-label">Clientes Atendidos</span>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/admin.js"></script>
</body>
</html>